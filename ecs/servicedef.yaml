serviceName: laravel
launchType: FARGATE
desiredCount: 1
networkConfiguration:
  awsvpcConfiguration:
    subnets:
      - <+input> # Harness will ask for your subnet IDs
    securityGroups:
      - <+input> # Harness will ask for your SG ID
    assignPublicIp: ENABLED
